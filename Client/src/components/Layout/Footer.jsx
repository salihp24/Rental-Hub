export default function Footer() {
  return (
    <footer className="mt-8 border-t border-blue-100 bg-white/75 backdrop-blur">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-8 text-sm text-slate-600 md:flex-row md:items-center md:justify-between">
        <div className="font-bold text-slate-800">Rental Hub</div>
        <div>Find reliable rentals with clear pricing and trusted listings.</div>
      </div>
    </footer>
  );
}
